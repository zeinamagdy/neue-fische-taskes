// Very simple, given a number, find its opposite.

import { test } from "@/test.ts";

function opposite(number: number): number {
  // your code here
}

test(opposite(1), -1);
test(opposite(-1), 1);
test(opposite(0), 0);
test(opposite(5), -5);
test(opposite(-100), 100);
