import { test } from "@/test.ts";

function summation(num: number): number {
  // your code here
}

test(summation(1), 1);
test(summation(5), 15);
test(summation(10), 55);
test(summation(3), 6);
test(summation(0), 0);
