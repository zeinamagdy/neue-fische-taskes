// This kata is about multiplying a given number by eight
// if it is an even number and by nine otherwise.

import { test } from "@/test.ts";

function simpleMultiplication(n: number): number {
  // your code here
}

test(simpleMultiplication(2), 16);
test(simpleMultiplication(3), 27);
test(simpleMultiplication(0), 0);
test(simpleMultiplication(5), 45);
test(simpleMultiplication(8), 64);
